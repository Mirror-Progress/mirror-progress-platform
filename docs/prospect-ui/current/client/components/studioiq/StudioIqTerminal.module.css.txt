.shell {
  --siq-bg-canvas: #090b0d;
  --siq-bg-grid: rgba(220, 231, 224, 0.035);
  --siq-surface-rail: rgba(17, 21, 24, 0.88);
  --siq-surface-raised: rgba(24, 29, 32, 0.94);
  --siq-surface-card: rgba(31, 36, 39, 0.78);
  --siq-surface-card-hover: rgba(39, 45, 48, 0.94);
  --siq-surface-selected: rgba(31, 45, 40, 0.96);
  --siq-surface-input: rgba(9, 11, 13, 0.72);
  --siq-surface-overlay: rgba(3, 5, 6, 0.74);
  --siq-field-ground: #090d0c;
  --siq-field-raised: #121a17;
  --siq-field-glass: rgba(12, 17, 16, 0.86);
  --siq-field-text: #f1f5f2;
  --siq-field-muted: #9eaea6;
  --siq-text-primary: #f1f5f2;
  --siq-text-secondary: #b2bab5;
  --siq-text-tertiary: #89938d;
  --siq-text-inverse: #09110c;
  --siq-line-subtle: rgba(218, 239, 225, 0.1);
  --siq-line-default: rgba(218, 239, 225, 0.16);
  --siq-line-strong: rgba(218, 239, 225, 0.27);
  --siq-accent-primary: #7fd3a3;
  --siq-accent-focus: #dfff3f;
  --siq-status-live: #dfff3f;
  --siq-status-warning: #f2b561;
  --siq-status-danger: #ff8a7a;
  --siq-status-info: #77afff;
  --siq-status-success: #7fd3a3;
  --siq-spectral-mineral: #7fd3a3;
  --siq-spectral-sulfur: #dfff3f;
  --siq-spectral-amber: #f2b561;
  --siq-spectral-cobalt: #77afff;
  --siq-spectral-ice: #9bdce8;
  --siq-spectral-danger: #ff8a7a;
  --siq-shadow-low: 0 2px 10px rgba(0, 0, 0, 0.28);
  --siq-shadow-mid: 0 12px 34px rgba(0, 0, 0, 0.38);
  --siq-shadow-high: 0 28px 72px rgba(0, 0, 0, 0.52);
  --siq-highlight-edge: inset 0 1px rgba(255, 255, 255, 0.055);
  --siq-ink: var(--siq-text-primary);
  --siq-forest: var(--siq-field-ground);
  --siq-forest-raised: var(--siq-field-raised);
  --siq-ground: var(--siq-bg-canvas);
  --siq-paper: var(--siq-surface-raised);
  --siq-surface: var(--siq-surface-rail);
  --siq-surface-strong: var(--siq-surface-raised);
  --siq-surface-muted: var(--siq-surface-card);
  --siq-border: var(--siq-line-default);
  --siq-border-strong: var(--siq-line-strong);
  --siq-border-dark: var(--siq-line-default);
  --siq-muted: var(--siq-text-secondary);
  --siq-accent: var(--siq-accent-primary);
  --siq-lime: var(--siq-status-live);
  --siq-amber: var(--siq-status-warning);
  --siq-danger: var(--siq-status-danger);
  --siq-radius-control: 4px;
  --siq-radius-card: 6px;
  --siq-radius-panel: 8px;
  --siq-space-1: 4px;
  --siq-space-2: 8px;
  --siq-space-3: 12px;
  --siq-space-4: 16px;
  --siq-space-5: 24px;
  --siq-space-6: 32px;
  --siq-control-compact: 32px;
  --siq-control: 40px;
  --siq-control-touch: 48px;
  --siq-fast: 140ms;
  --siq-select: 220ms;
  --siq-relate: 320ms;
  --siq-assemble: 520ms;
  --siq-ease-feedback: cubic-bezier(0.2, 0, 0, 1);
  --siq-ease-select: cubic-bezier(0.22, 1, 0.36, 1);
  --siq-ease-relate: cubic-bezier(0.16, 1, 0.3, 1);
  --siq-shadow-1: var(--siq-shadow-low);
  --siq-shadow-2: var(--siq-shadow-mid);
  --siq-shadow-3: var(--siq-shadow-high);
  --ink: var(--siq-ink);
  --forest: var(--siq-forest);
  --forest-2: var(--siq-forest-raised);
  --line: var(--siq-border);
  --line-dark: var(--siq-border-dark);
  --paper: var(--siq-paper);
  --white: var(--siq-text-primary);
  --muted: var(--siq-muted);
  --lime: var(--siq-lime);
  --amber: var(--siq-amber);
  width: 100%;
  max-width: 100%;
  min-height: 100vh;
  min-height: 100dvh;
  overflow-x: clip;
  color: var(--siq-ink);
  background:
    linear-gradient(var(--siq-bg-grid) 1px, transparent 1px),
    linear-gradient(90deg, var(--siq-bg-grid) 1px, transparent 1px),
    var(--siq-ground);
  background-size: 24px 24px;
  font-family:
    var(--font-mp-sans, 'DM Sans'),
    ui-sans-serif,
    system-ui,
    -apple-system,
    BlinkMacSystemFont,
    'Segoe UI',
    sans-serif;
  letter-spacing: 0;
}

:global(:root[data-theme='light']) .shell {
  --siq-bg-canvas: #f2f3ef;
  --siq-bg-grid: rgba(7, 17, 12, 0.022);
  --siq-surface-rail: rgba(250, 251, 248, 0.82);
  --siq-surface-raised: rgba(255, 255, 252, 0.9);
  --siq-surface-card: rgba(255, 255, 255, 0.74);
  --siq-surface-card-hover: rgba(255, 255, 255, 0.96);
  --siq-surface-selected: rgba(226, 240, 231, 0.92);
  --siq-surface-input: rgba(255, 255, 255, 0.78);
  --siq-surface-overlay: rgba(7, 17, 12, 0.28);
  --siq-field-ground: #0a1710;
  --siq-field-raised: #102219;
  --siq-field-glass: rgba(16, 34, 25, 0.86);
  --siq-field-text: #f4f8f4;
  --siq-field-muted: #9eaea6;
  --siq-text-primary: #07110c;
  --siq-text-secondary: #5a675f;
  --siq-text-tertiary: #728078;
  --siq-text-inverse: #f4f8f4;
  --siq-line-subtle: rgba(21, 42, 31, 0.09);
  --siq-line-default: rgba(21, 42, 31, 0.14);
  --siq-line-strong: rgba(21, 42, 31, 0.24);
  --siq-accent-primary: #2f6b4f;
  --siq-accent-focus: #245fb7;
  --siq-status-live: #6b7900;
  --siq-status-warning: #9b5e15;
  --siq-status-danger: #a54237;
  --siq-status-info: #245fb7;
  --siq-status-success: #2f6b4f;
  --siq-spectral-mineral: #2f6b4f;
  --siq-spectral-sulfur: #6b7900;
  --siq-spectral-amber: #9b5e15;
  --siq-spectral-cobalt: #245fb7;
  --siq-spectral-ice: #2e7181;
  --siq-spectral-danger: #a54237;
  --siq-shadow-low: 0 2px 8px rgba(7, 17, 12, 0.07);
  --siq-shadow-mid: 0 10px 28px rgba(7, 17, 12, 0.11);
  --siq-shadow-high: 0 24px 64px rgba(7, 17, 12, 0.18);
  --siq-highlight-edge: inset 0 1px rgba(255, 255, 255, 0.75);
}

.shell *,
.shell *::before,
.shell *::after {
  box-sizing: border-box;
}
.shell,
.signalRail,
.intelligenceRail,
.pageIntro,
.practiceFilters,
.practiceGrid,
.teamRegister {
  transition:
    background-color var(--siq-select) var(--siq-ease-feedback),
    border-color var(--siq-select) var(--siq-ease-feedback),
    color var(--siq-select) var(--siq-ease-feedback),
    box-shadow var(--siq-select) var(--siq-ease-feedback);
}
.shell button,
.shell input,
.shell select,
.shell textarea {
  font: inherit;
}
.shell button:focus-visible,
.shell input:focus-visible,
.shell select:focus-visible,
.shell textarea:focus-visible,
.shell a:focus-visible,
.signals:focus-visible,
.inspectorBody:focus-visible {
  outline: 2px solid var(--siq-accent-focus);
  outline-offset: 2px;
}

.header {
  position: relative;
  z-index: 10;
  height: 64px;
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  align-items: center;
  gap: var(--siq-space-3);
  padding: var(--siq-space-2) var(--siq-space-3);
  color: var(--siq-field-text);
  background: var(--siq-field-glass);
  border-bottom: 1px solid var(--siq-line-subtle);
  box-shadow: var(--siq-shadow-mid);
  backdrop-filter: blur(24px) saturate(1.08);
  min-width: 0;
}
.brand {
  min-width: 0;
  height: var(--siq-control-touch);
  display: flex;
  align-items: center;
  gap: var(--siq-space-3);
  padding: 0 var(--siq-space-2);
  color: inherit;
  text-decoration: none;
}
.brand > span {
  display: grid;
  place-items: center;
  width: 34px;
  height: 34px;
  flex: 0 0 34px;
  border-radius: var(--siq-radius-control);
  background: var(--siq-lime);
  color: var(--siq-text-inverse);
  box-shadow: var(--siq-shadow-low);
  font:
    800 12px/1 var(--font-mp-mono, 'Diatype Mono'),
    ui-monospace,
    monospace;
}
.brand div {
  min-width: 0;
  display: grid;
  gap: 2px;
}
.brand strong {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  font-size: 20px;
  line-height: 1.1;
}
.brand small {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  color: var(--siq-field-muted);
  font-size: 12px;
}
.header nav {
  justify-self: start;
  min-width: 0;
  height: var(--siq-control);
  display: flex;
  align-items: center;
  gap: 2px;
  padding: var(--siq-space-1);
  border: 1px solid rgba(210, 229, 216, 0.16);
  border-radius: var(--siq-radius-card);
  background: rgba(255, 255, 255, 0.055);
  box-shadow: inset 0 1px rgba(255, 255, 255, 0.045);
}
.header nav button {
  position: relative;
  min-height: var(--siq-control-compact);
  padding: 0 var(--siq-space-3);
  border: 0;
  border-radius: var(--siq-radius-control);
  background: transparent;
  color: var(--siq-field-muted);
  font:
    650 11px/1 var(--font-mp-sans, 'DM Sans'),
    sans-serif;
  cursor: pointer;
  transition:
    color var(--siq-fast) var(--siq-ease-feedback),
    background-color var(--siq-fast) var(--siq-ease-feedback),
    box-shadow var(--siq-select) var(--siq-ease-select),
    transform var(--siq-fast) var(--siq-ease-feedback);
}
.header nav button:hover {
  color: var(--siq-field-text);
  background: var(--siq-line-subtle);
}
.header nav button:active {
  transform: translateY(1px);
}
.header nav .activeNav {
  color: var(--siq-ink);
  background: var(--siq-surface-strong);
  box-shadow: var(--siq-shadow-low), var(--siq-highlight-edge);
}
.header nav .activeNav::after {
  content: '';
  position: absolute;
  left: 50%;
  bottom: 3px;
  width: 4px;
  height: 4px;
  transform: translateX(-50%);
  border-radius: 50%;
  background: var(--siq-accent);
}
.header nav i {
  min-width: 18px;
  height: 18px;
  display: inline-grid;
  place-items: center;
  margin-left: 6px;
  border-radius: 50%;
  background: rgba(72, 107, 87, 0.2);
  color: currentColor;
  font:
    700 9px/1 var(--font-mp-mono, 'Diatype Mono'),
    ui-monospace,
    monospace;
}
.accountMenu {
  position: relative;
  min-width: 0;
}
.identity,
.menuToggle,
.themeToggle {
  width: var(--siq-control);
  height: var(--siq-control);
  flex: 0 0 var(--siq-control);
  display: grid;
  place-items: center;
  border: 1px solid var(--siq-line-default);
  border-radius: var(--siq-radius-control);
  background: var(--siq-line-subtle);
  color: var(--siq-field-text);
  box-shadow: var(--siq-highlight-edge);
  cursor: pointer;
  transition:
    color var(--siq-fast) var(--siq-ease-feedback),
    background-color var(--siq-fast) var(--siq-ease-feedback),
    border-color var(--siq-fast) var(--siq-ease-feedback),
    transform var(--siq-fast) var(--siq-ease-feedback);
}
.identity {
  position: relative;
  text-decoration: none;
}
.identity:hover,
.menuToggle:hover,
.menuToggle[aria-expanded='true'],
.themeToggle:hover {
  transform: translateY(-1px);
  border-color: var(--siq-line-strong);
  background: rgba(255, 255, 255, 0.08);
}
.identity > svg {
  width: 21px;
  height: 21px;
}
.identity > span {
  position: absolute;
  right: 3px;
  bottom: 3px;
  min-width: 13px;
  height: 13px;
  display: grid;
  place-items: center;
  padding: 0 2px;
  border: 1px solid var(--siq-field-ground);
  border-radius: 50%;
  background: var(--siq-lime);
  color: var(--siq-text-inverse);
  font:
    800 8px/1 var(--font-mp-mono, 'Diatype Mono'),
    ui-monospace,
    monospace;
}
.menuToggle svg,
.themeToggle svg {
  width: 17px;
  height: 17px;
}
.accountPopover {
  position: absolute;
  z-index: 40;
  top: calc(100% + 8px);
  right: 0;
  width: 274px;
  overflow: hidden;
  padding: 6px;
  border: 1px solid var(--siq-line-strong);
  border-radius: var(--siq-radius-card);
  background: color-mix(in srgb, var(--siq-surface-raised) 94%, transparent);
  color: var(--siq-text-primary);
  box-shadow: var(--siq-shadow-high), var(--siq-highlight-edge);
  backdrop-filter: blur(22px) saturate(1.1);
  animation: accountReveal var(--siq-select) var(--siq-ease-select) both;
}
.accountPopover > div {
  display: grid;
  gap: 4px;
  padding: var(--siq-space-3);
  border-bottom: 1px solid var(--siq-line-default);
}
.accountPopover > div span {
  color: var(--siq-accent-primary);
  font:
    750 11px/1.1 var(--font-mp-mono, 'Diatype Mono'),
    ui-monospace,
    monospace;
  text-transform: uppercase;
}
.accountPopover > div strong {
  overflow: hidden;
  font-size: 14px;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.accountPopover > div small {
  overflow: hidden;
  color: var(--siq-text-tertiary);
  font-size: 12px;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.accountPopover > a {
  min-height: 42px;
  display: flex;
  align-items: center;
  gap: 10px;
  margin-top: 4px;
  padding: 0 var(--siq-space-3);
  border: 1px solid transparent;
  border-radius: var(--siq-radius-control);
  color: var(--siq-text-secondary);
  font-size: 13px;
  font-weight: 700;
  text-decoration: none;
}
.accountPopover > a:hover {
  border-color: var(--siq-line-default);
  background: var(--siq-surface-selected);
  color: var(--siq-text-primary);
}
.accountPopover > a svg {
  width: 18px;
  color: var(--siq-accent-primary);
}
@keyframes accountReveal {
  from {
    opacity: 0;
    transform: translateY(-6px) scale(0.985);
  }
  to {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}
.headerActions {
  min-width: 0;
  justify-self: end;
  display: flex;
  align-items: center;
  gap: var(--siq-space-2);
}

.error {
  position: fixed;
  z-index: 30;
  top: 76px;
  left: 50%;
  transform: translateX(-50%);
  width: min(560px, calc(100vw - 32px));
  display: flex;
  gap: var(--siq-space-4);
  align-items: center;
  justify-content: space-between;
  padding: var(--siq-space-3) var(--siq-space-4);
  border: 1px solid rgba(169, 78, 53, 0.75);
  border-radius: var(--siq-radius-card);
  background: rgba(41, 22, 15, 0.95);
  color: #ffd6c7;
  box-shadow: var(--siq-shadow-3);
  backdrop-filter: blur(18px);
  font-size: 13px;
  overflow-wrap: anywhere;
}
.error button {
  min-height: var(--siq-control-compact);
  padding: 0 var(--siq-space-2);
  border: 0;
  border-radius: var(--siq-radius-control);
  color: var(--siq-lime);
  background: transparent;
  font-weight: 700;
  cursor: pointer;
}

.cockpit {
  height: calc(100vh - 64px);
  height: calc(100dvh - 64px);
  min-width: 0;
  min-height: 0;
  display: grid;
  grid-template-columns: clamp(276px, 21vw, 344px) minmax(480px, 1fr) clamp(
      324px,
      24vw,
      390px
    );
  grid-template-rows: minmax(0, 1fr);
  gap: var(--siq-space-3);
  padding: var(--siq-space-3);
  overflow: hidden;
}
.signalRail,
.field,
.intelligenceRail {
  min-width: 0;
  min-height: 0;
  height: auto;
  max-height: none;
  border-radius: var(--siq-radius-panel);
  overflow: hidden;
  box-shadow: var(--siq-shadow-2);
}
.signalRail,
.intelligenceRail {
  background: var(--siq-surface);
  border: 1px solid var(--siq-line-default);
  backdrop-filter: blur(24px) saturate(1.12);
}
.signalRail {
  display: grid;
  grid-template-rows: auto auto auto minmax(0, 1fr);
}
.railHead {
  display: flex;
  justify-content: space-between;
  gap: var(--siq-space-4);
  padding: var(--siq-space-5) var(--siq-space-4) var(--siq-space-3);
}
.railHead h1,
.pageIntro h1 {
  margin: var(--siq-space-1) 0 0;
  font-size: 24px;
  line-height: 1.05;
  font-weight: 650;
}
.railHead > button {
  align-self: flex-start;
  width: var(--siq-control-compact);
  height: var(--siq-control-compact);
  display: grid;
  place-items: center;
  border: 1px solid var(--siq-border);
  border-radius: var(--siq-radius-control);
  background: var(--siq-surface-strong);
  color: var(--siq-ink);
  box-shadow: var(--siq-shadow-1);
  cursor: pointer;
  transition:
    transform var(--siq-fast) var(--siq-ease-feedback),
    box-shadow var(--siq-fast) var(--siq-ease-feedback),
    border-color var(--siq-fast) var(--siq-ease-feedback);
}
.railHead > button:hover {
  transform: translateY(-1px);
  border-color: var(--siq-border-strong);
  box-shadow: var(--siq-shadow-2);
}
.railHead > button svg {
  width: 14px;
}
.kicker {
  color: var(--siq-text-secondary);
  text-transform: uppercase;
  font:
    700 9px/1.2 var(--font-mp-mono, 'Diatype Mono'),
    ui-monospace,
    monospace;
}
.search {
  display: grid;
  grid-template-columns: 20px minmax(0, 1fr) auto;
  align-items: center;
  gap: var(--siq-space-1);
  height: var(--siq-control);
  margin: 0 var(--siq-space-4) var(--siq-space-3);
  padding: 0 var(--siq-space-2);
  border: 1px solid var(--siq-border-strong);
  border-radius: var(--siq-radius-control);
  background: var(--siq-surface-input);
  box-shadow: var(--siq-highlight-edge);
  transition:
    border-color var(--siq-fast) var(--siq-ease-feedback),
    box-shadow var(--siq-fast) var(--siq-ease-feedback);
}
.search:focus-within {
  border-color: var(--siq-accent);
  box-shadow: 0 0 0 2px rgba(72, 107, 87, 0.12);
}
.search svg {
  width: 14px;
  color: var(--siq-muted);
}
.search input,
.search select {
  min-width: 0;
  height: 100%;
  border: 0;
  outline: 0;
  background: transparent;
  color: var(--siq-ink);
  font:
    600 10px/1 var(--font-mp-sans, 'DM Sans'),
    sans-serif;
}
.search select {
  max-width: 112px;
  padding-left: var(--siq-space-2);
  border-left: 1px solid var(--siq-border);
}
.signalCount {
  display: flex;
  justify-content: space-between;
  gap: var(--siq-space-2);
  margin: 0 var(--siq-space-4) var(--siq-space-2);
  padding: var(--siq-space-2) 0;
  border-top: 1px solid var(--siq-border);
  color: var(--siq-muted);
  font:
    650 9px/1 var(--font-mp-mono, 'Diatype Mono'),
    ui-monospace,
    monospace;
  text-transform: uppercase;
}
.signals {
  min-height: 0;
  overflow-x: hidden;
  overflow-y: auto;
  overscroll-behavior: contain;
  scrollbar-gutter: stable;
  scrollbar-width: thin;
  scrollbar-color: rgba(72, 107, 87, 0.55) transparent;
  padding: 0 var(--siq-space-3) var(--siq-space-3);
}
.signals::-webkit-scrollbar,
.inspectorBody::-webkit-scrollbar {
  width: 8px;
}
.signals::-webkit-scrollbar-track,
.inspectorBody::-webkit-scrollbar-track {
  background: transparent;
}
.signals::-webkit-scrollbar-thumb,
.inspectorBody::-webkit-scrollbar-thumb {
  border: 2px solid transparent;
  border-radius: var(--siq-radius-control);
  background: rgba(72, 107, 87, 0.52);
  background-clip: padding-box;
}
.signals > button {
  position: relative;
  width: 100%;
  min-height: 106px;
  display: grid;
  grid-template-columns: 28px minmax(0, 1fr) 42px;
  gap: var(--siq-space-2);
  margin-top: var(--siq-space-2);
  padding: var(--siq-space-3);
  text-align: left;
  border: 1px solid var(--siq-border);
  border-radius: var(--siq-radius-card);
  background: linear-gradient(
    110deg,
    var(--siq-surface-card),
    color-mix(in srgb, var(--siq-spectral-cobalt) 4%, transparent)
  );
  color: inherit;
  box-shadow: var(--siq-shadow-1), var(--siq-highlight-edge);
  cursor: pointer;
  animation: signalArrive var(--siq-relate) var(--siq-ease-relate) both;
  transition:
    background-color var(--siq-fast) var(--siq-ease-feedback),
    border-color var(--siq-fast) var(--siq-ease-feedback),
    box-shadow var(--siq-select) var(--siq-ease-select),
    transform var(--siq-select) var(--siq-ease-select);
}
.signals > button:nth-child(2) {
  animation-delay: 30ms;
}
.signals > button:nth-child(3) {
  animation-delay: 60ms;
}
.signals > button:nth-child(4) {
  animation-delay: 90ms;
}
.signals > button:nth-child(5) {
  animation-delay: 120ms;
}
.signals > button:nth-child(6) {
  animation-delay: 150ms;
}
.signals > button:nth-child(n + 7) {
  animation-delay: 180ms;
}
.signals > button:hover {
  z-index: 1;
  transform: translateY(-1px);
  border-color: var(--siq-border-strong);
  background: var(--siq-surface-card-hover);
  box-shadow: var(--siq-shadow-2);
}
.signals > button.selectedSignal {
  z-index: 2;
  transform: translateY(-1px);
  border-color: var(--siq-spectral-mineral);
  background: linear-gradient(
    112deg,
    var(--siq-surface-selected),
    color-mix(in srgb, var(--siq-spectral-cobalt) 9%, transparent)
  );
  box-shadow:
    var(--siq-shadow-2),
    inset 0 1px color-mix(in srgb, var(--siq-spectral-ice) 18%, transparent);
}
.signals > button.selectedSignal::before {
  content: '';
  position: absolute;
  top: var(--siq-space-2);
  bottom: var(--siq-space-2);
  left: 0;
  width: 3px;
  border-radius: 0 3px 3px 0;
  background: var(--siq-lime);
  box-shadow: 0 0 10px rgba(141, 167, 0, 0.22);
}
.rank {
  color: var(--siq-text-tertiary);
  padding-top: 2px;
  font:
    700 9px/1 var(--font-mp-mono, 'Diatype Mono'),
    ui-monospace,
    monospace;
}
.signals > button > div {
  min-width: 0;
  display: grid;
  align-content: start;
  gap: 6px;
}
.signals strong {
  font-size: 13px;
  line-height: 1.28;
  font-weight: 680;
}
.signals small,
.signals em {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  color: var(--siq-muted);
  font-size: 10px;
  font-style: normal;
}
.signals em {
  color: var(--siq-accent-primary);
  font-weight: 650;
}
.badges {
  display: flex;
  flex-wrap: wrap;
  gap: var(--siq-space-1);
  min-height: 17px;
}
.badges i {
  padding: 3px 5px;
  border: 1px solid var(--siq-line-strong);
  border-radius: var(--siq-radius-control);
  color: var(--siq-text-secondary);
  background: var(--siq-line-subtle);
  font:
    700 8px/1 var(--font-mp-mono, 'Diatype Mono'),
    ui-monospace,
    monospace;
  text-transform: uppercase;
  font-style: normal;
}
.rowScore {
  align-self: center;
  text-align: center;
  color: var(--siq-ink);
  font:
    760 17px/1 var(--font-mp-mono, 'Diatype Mono'),
    ui-monospace,
    monospace;
}
.rowScore small {
  display: block;
  margin-top: var(--siq-space-1);
  color: var(--siq-muted);
  text-transform: uppercase;
  font-size: 8px;
}
.loader {
  min-height: 160px;
  display: flex;
  gap: var(--siq-space-2);
  align-items: center;
  justify-content: center;
  color: var(--siq-muted);
  font:
    700 9px/1 var(--font-mp-mono, 'Diatype Mono'),
    ui-monospace,
    monospace;
  text-transform: uppercase;
}
.loader i {
  width: 7px;
  height: 7px;
  border-radius: 50%;
  background: var(--siq-accent);
  animation: pulse 1s infinite alternate;
}

.field {
  position: relative;
  display: grid;
  grid-template-rows: 56px minmax(0, 1fr) auto 32px;
  color: #fff;
  background: var(--siq-forest);
  border: 1px solid var(--siq-line-default);
  box-shadow: 0 26px 80px rgba(0, 0, 0, 0.36);
}
.fieldTop {
  position: relative;
  z-index: 2;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--siq-space-2);
  padding: 0 var(--siq-space-3);
  border-bottom: 1px solid var(--siq-border-dark);
  background: rgba(7, 17, 12, 0.84);
  backdrop-filter: blur(16px);
}
.modeSwitch {
  height: var(--siq-control-compact);
  display: flex;
  gap: 2px;
  padding: 3px;
  border: 1px solid rgba(196, 223, 205, 0.2);
  border-radius: var(--siq-radius-card);
  background: rgba(255, 255, 255, 0.055);
}
.modeSwitch button {
  min-height: 24px;
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 0 var(--siq-space-2);
  border: 0;
  border-radius: var(--siq-radius-control);
  background: transparent;
  color: #9db0a4;
  font:
    700 9px/1 var(--font-mp-sans, 'DM Sans'),
    sans-serif;
  cursor: pointer;
  transition:
    color var(--siq-fast) var(--siq-ease-feedback),
    background-color var(--siq-fast) var(--siq-ease-feedback),
    box-shadow var(--siq-fast) var(--siq-ease-feedback);
}
.modeSwitch button svg {
  width: 13px;
}
.modeSwitch .modeActive {
  color: var(--siq-ink);
  background: var(--siq-lime);
  box-shadow: 0 3px 10px rgba(0, 0, 0, 0.2);
}
.fieldMetrics {
  display: flex;
  gap: var(--siq-space-3);
}
.fieldMetrics span {
  color: #8da397;
  font:
    650 8px/1 var(--font-mp-mono, 'Diatype Mono'),
    ui-monospace,
    monospace;
  text-transform: uppercase;
}
.fieldMetrics strong {
  margin-right: 3px;
  color: #fff;
  font-size: 11px;
}
.visualField {
  position: relative;
  min-height: 0;
  overflow: hidden;
  background: linear-gradient(138deg, #080d12 0%, #101b18 44%, #111411 100%);
}
.primaryInstrument {
  position: absolute;
  z-index: 1;
  inset: 7% 9% 9%;
  opacity: 0.92;
  transition:
    opacity var(--siq-select) var(--siq-ease-select),
    transform var(--siq-relate) var(--siq-ease-relate);
}
.fieldReadout {
  position: absolute;
  z-index: 3;
  top: 10%;
  left: 8%;
  display: grid;
  align-content: start;
  color: var(--siq-field-text);
  text-shadow: 0 1px 16px rgba(0, 0, 0, 0.48);
  pointer-events: none;
}
.fieldReadout > span {
  color: var(--siq-spectral-ice);
  font:
    400 10px/1 var(--font-mp-mono, 'Diatype Mono'),
    ui-monospace,
    monospace;
  text-transform: uppercase;
}
.fieldReadout > strong {
  margin-top: 12px;
  font:
    400 clamp(42px, 5vw, 72px) / 0.88 var(--font-mp-sans, 'DM Sans'),
    sans-serif;
  font-variant-numeric: tabular-nums;
}
.fieldReadout > small {
  margin-top: 8px;
  color: var(--siq-field-muted);
  font-size: 11px;
  text-transform: uppercase;
}
.fieldSelection {
  position: relative;
  z-index: 2;
  min-height: 84px;
  display: grid;
  grid-template-columns: 52px minmax(0, 1fr) minmax(90px, 24%) 32px;
  align-items: center;
  gap: var(--siq-space-3);
  margin: 0 var(--siq-space-3) var(--siq-space-3);
  padding: var(--siq-space-3);
  border: 1px solid rgba(171, 204, 182, 0.22);
  border-radius: var(--siq-radius-card);
  background: rgba(14, 21, 19, 0.84);
  box-shadow:
    0 14px 36px rgba(0, 0, 0, 0.28),
    inset 0 1px rgba(255, 255, 255, 0.055);
  backdrop-filter: blur(18px) saturate(1.12);
  animation: rise var(--siq-select) var(--siq-ease-select) both;
}
.fieldSelection > span {
  width: 44px;
  height: 44px;
  display: grid;
  place-items: center;
  border: 1px solid #52735f;
  border-radius: var(--siq-radius-control);
  color: var(--siq-lime);
  background: rgba(220, 255, 41, 0.04);
  font:
    750 17px/1 var(--font-mp-mono, 'Diatype Mono'),
    ui-monospace,
    monospace;
}
.fieldSelection div {
  min-width: 0;
  display: grid;
  gap: var(--siq-space-1);
}
.fieldSelection small,
.fieldSelection em {
  color: #9caf9f;
  font-size: 9px;
  font-style: normal;
  text-transform: uppercase;
}
.fieldSelection strong {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  font-size: 12px;
}
.selectionTrace {
  position: relative;
  height: 48px;
  overflow: hidden;
  border-left: 1px solid rgba(220, 235, 225, 0.16);
}
.fieldSelection button {
  width: var(--siq-control-compact);
  height: var(--siq-control-compact);
  display: grid;
  place-items: center;
  border: 1px solid #4a6656;
  border-radius: var(--siq-radius-control);
  background: rgba(255, 255, 255, 0.045);
  color: #fff;
  cursor: pointer;
  transition:
    background-color var(--siq-fast) var(--siq-ease-feedback),
    transform var(--siq-fast) var(--siq-ease-feedback);
}
.fieldSelection button:hover {
  transform: translateY(-1px);
  background: rgba(255, 255, 255, 0.1);
}
.fieldSelection button svg {
  width: 13px;
}
.fieldStatus {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  align-items: center;
  padding: 0 var(--siq-space-3);
  border-top: 1px solid var(--siq-border-dark);
  background: rgba(7, 17, 12, 0.96);
}
.fieldStatus span {
  color: #8da397;
  font:
    650 8px/1 var(--font-mp-mono, 'Diatype Mono'),
    ui-monospace,
    monospace;
  text-transform: uppercase;
}
.fieldStatus i {
  display: inline-block;
  width: 4px;
  height: 4px;
  margin-right: var(--siq-space-1);
  border-radius: 50%;
  background: #789083;
}
.fieldStatus .statusReady {
  color: var(--siq-lime);
}
.fieldStatus .statusReady i {
  background: var(--siq-lime);
  box-shadow: 0 0 6px var(--siq-lime);
}

.sonar {
  position: absolute;
  width: min(74%, 520px);
  aspect-ratio: 1;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
  border: 1px solid #42604f;
  border-radius: 50%;
}
.sonar > i {
  position: absolute;
  inset: 16.66%;
  border: 1px solid #365043;
  border-radius: 50%;
}
.sonar > i:nth-of-type(2) {
  inset: 33.33%;
}
.sonar > i:nth-of-type(3) {
  inset: 49.5%;
}
.sonar::before,
.sonar::after {
  content: '';
  position: absolute;
  background: #365043;
  opacity: 0.7;
}
.sonar::before {
  width: 1px;
  height: 100%;
  left: 50%;
}
.sonar::after {
  height: 1px;
  width: 100%;
  top: 50%;
}
.sonarSweep {
  position: absolute;
  inset: 0;
  border-radius: 50%;
  border-top: 1px solid var(--siq-lime);
  animation: spin 5s linear infinite;
  opacity: 0.75;
}
.sonar button {
  position: absolute;
  z-index: 2;
  width: 12px;
  height: 12px;
  padding: 0;
  transform: translate(-50%, -50%);
  border: 3px solid transparent;
  border-radius: 50%;
  background: var(--siq-amber);
  background-clip: padding-box;
  box-shadow: 0 0 10px rgba(213, 154, 86, 0.7);
  cursor: pointer;
  transition:
    transform var(--siq-select) var(--siq-ease-select),
    box-shadow var(--siq-select) var(--siq-ease-select);
}
.sonar button:hover {
  transform: translate(-50%, -50%) scale(1.3);
}
.sonar button.activeSignal {
  width: 16px;
  height: 16px;
  background: var(--siq-lime);
  background-clip: padding-box;
  box-shadow: 0 0 15px var(--siq-lime);
}
.sonarLabel {
  position: absolute;
  z-index: 3;
  width: 90px;
  height: 90px;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
  display: grid;
  place-items: center;
  align-content: center;
  gap: 2px;
  border: 1px solid #41604e;
  border-radius: 50%;
  background: rgba(11, 27, 19, 0.94);
  box-shadow: 0 12px 32px rgba(0, 0, 0, 0.26);
}
.sonarLabel svg {
  width: 14px;
  color: var(--siq-lime);
}
.sonarLabel strong {
  font:
    750 22px/1 var(--font-mp-mono, 'Diatype Mono'),
    ui-monospace,
    monospace;
}
.sonarLabel span {
  color: #98aa9f;
  font-size: 8px;
  text-transform: uppercase;
}

.intelligenceRail {
  display: grid;
  grid-template-rows: auto auto minmax(0, 1fr);
}
.inspectorClose,
.inspectorBackdrop {
  display: none;
}
.inspectorTabs {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 2px;
  margin: var(--siq-space-2);
  padding: 3px;
  border: 1px solid var(--siq-border);
  border-radius: var(--siq-radius-card);
  background: var(--siq-line-subtle);
}
.inspectorTabs button {
  position: relative;
  min-height: var(--siq-control-compact);
  border: 0;
  border-radius: var(--siq-radius-control);
  background: transparent;
  color: var(--siq-muted);
  font:
    700 9px/1 var(--font-mp-sans, 'DM Sans'),
    sans-serif;
  cursor: pointer;
  transition:
    color var(--siq-fast) var(--siq-ease-feedback),
    background-color var(--siq-fast) var(--siq-ease-feedback),
    box-shadow var(--siq-select) var(--siq-ease-select);
}
.inspectorTabs button:hover {
  color: var(--siq-ink);
  background: var(--siq-surface-card-hover);
}
.inspectorTabs .inspectorActive {
  color: var(--siq-ink);
  background: var(--siq-surface-strong);
  box-shadow: var(--siq-shadow-1);
}
.inspectorTabs .inspectorActive::after {
  content: '';
  position: absolute;
  left: 50%;
  bottom: 3px;
  width: 4px;
  height: 4px;
  transform: translateX(-50%);
  border-radius: 50%;
  background: var(--siq-accent);
}
.inspectorTitle {
  position: relative;
  z-index: 2;
  padding: var(--siq-space-3) var(--siq-space-4) var(--siq-space-4);
  border-bottom: 1px solid var(--siq-border);
  background: var(--siq-surface-rail);
  backdrop-filter: blur(18px);
}
.inspectorTitle > span {
  color: var(--siq-muted);
  font:
    650 8px/1 var(--font-mp-mono, 'Diatype Mono'),
    ui-monospace,
    monospace;
  text-transform: uppercase;
}
.inspectorTitle h2 {
  margin: 7px 0 var(--siq-space-3);
  font-size: 17px;
  line-height: 1.25;
  font-weight: 680;
}
.inspectorTitle > div {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 1px;
  overflow: hidden;
  border: 1px solid var(--siq-border);
  border-radius: var(--siq-radius-card);
  background: var(--siq-border);
  box-shadow: var(--siq-shadow-1);
}
.inspectorTitle > div strong {
  min-width: 0;
  padding: var(--siq-space-2);
  background: var(--siq-surface-raised);
  font:
    750 15px/1 var(--font-mp-mono, 'Diatype Mono'),
    ui-monospace,
    monospace;
}
.inspectorTitle > div small {
  display: block;
  margin-top: var(--siq-space-1);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  color: var(--siq-muted);
  font:
    650 8px/1 var(--font-mp-sans, 'DM Sans'),
    sans-serif;
  text-transform: uppercase;
}
.inspectorBody {
  min-height: 0;
  overflow-x: hidden;
  overflow-y: auto;
  overscroll-behavior: contain;
  scrollbar-gutter: stable;
  scrollbar-width: thin;
  scrollbar-color: rgba(72, 107, 87, 0.55) transparent;
  padding: var(--siq-space-4) var(--siq-space-4) 64px;
  animation: panelReveal var(--siq-relate) var(--siq-ease-relate) both;
}
.inspectorBody > p {
  margin: 0 0 var(--siq-space-4);
  color: var(--siq-text-secondary);
  font-size: 12px;
  line-height: 1.6;
}
.inspectorBody dl {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 1px;
  margin: 0 0 var(--siq-space-4);
  overflow: hidden;
  border: 1px solid var(--siq-border);
  border-radius: var(--siq-radius-card);
  background: var(--siq-border);
  box-shadow: var(--siq-shadow-1);
}
.inspectorBody dl div {
  min-width: 0;
  padding: var(--siq-space-2);
  background: var(--siq-surface-card);
}
.inspectorBody dt {
  color: var(--siq-muted);
  font-size: 8px;
  text-transform: uppercase;
}
.inspectorBody dd {
  margin: 5px 0 0;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  font-size: 10px;
  font-weight: 700;
  text-transform: capitalize;
}
.inspectorBody section {
  margin-top: var(--siq-space-5);
}
.inspectorBody h3 {
  margin: 0 0 var(--siq-space-3);
  color: var(--siq-text-secondary);
  font:
    750 9px/1 var(--font-mp-mono, 'Diatype Mono'),
    ui-monospace,
    monospace;
  text-transform: uppercase;
}
.inspectorBody ul,
.inspectorBody ol {
  margin: 0;
  padding-left: 18px;
}
.inspectorBody li {
  margin-bottom: var(--siq-space-2);
  color: var(--siq-text-secondary);
  font-size: 11px;
  line-height: 1.5;
}
.workspaceLink {
  min-height: 62px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--siq-space-3);
  margin-bottom: var(--siq-space-4);
  padding: var(--siq-space-3);
  border: 1px solid var(--siq-accent-primary);
  border-radius: var(--siq-radius-card);
  background: var(--siq-surface-selected);
  color: var(--siq-text-primary);
  text-decoration: none;
  box-shadow: var(--siq-shadow-mid), var(--siq-highlight-edge);
  transition:
    transform var(--siq-fast) var(--siq-ease-feedback),
    border-color var(--siq-fast) var(--siq-ease-feedback),
    box-shadow var(--siq-select) var(--siq-ease-select);
}
.workspaceLink:hover {
  transform: translateY(-1px);
  border-color: var(--siq-accent-focus);
  box-shadow: var(--siq-shadow-high), var(--siq-highlight-edge);
}
.workspaceLink span {
  min-width: 0;
  display: grid;
  gap: var(--siq-space-1);
}
.workspaceLink strong {
  font-size: 11px;
}
.workspaceLink small {
  overflow: hidden;
  color: var(--siq-text-secondary);
  font-size: 9px;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.workspaceLink svg {
  width: 16px;
  flex: 0 0 16px;
  color: var(--siq-accent-focus);
}
.match {
  position: relative;
  overflow: hidden;
  padding: var(--siq-space-3);
  border: 1px solid var(--siq-line-strong);
  border-radius: var(--siq-radius-card);
  background: var(--siq-surface-card);
  box-shadow: var(--siq-shadow-1);
  transition:
    transform var(--siq-select) var(--siq-ease-select),
    border-color var(--siq-fast) var(--siq-ease-feedback),
    box-shadow var(--siq-select) var(--siq-ease-select);
}
.match:hover {
  transform: translateY(-1px);
  border-color: rgba(72, 107, 87, 0.42);
  box-shadow: var(--siq-shadow-2);
}
.match > img {
  width: calc(100% + 24px);
  aspect-ratio: 16 / 8;
  object-fit: cover;
  display: block;
  margin: -12px -12px var(--siq-space-3);
  filter: saturate(0.82) contrast(1.03);
  transition: filter var(--siq-relate) var(--siq-ease-relate);
}
.match:hover > img {
  filter: saturate(1) contrast(1.02);
}
.match + .match {
  margin-top: var(--siq-space-2);
}
.match > div {
  display: flex;
  justify-content: space-between;
  gap: var(--siq-space-2);
  margin-bottom: var(--siq-space-2);
}
.match > div span,
.match > div em {
  color: var(--siq-accent-primary);
  font:
    700 8px/1 var(--font-mp-mono, 'Diatype Mono'),
    ui-monospace,
    monospace;
  text-transform: uppercase;
  font-style: normal;
}
.match strong {
  font-size: 12px;
  line-height: 1.35;
}
.match > small {
  display: block;
  margin-top: var(--siq-space-1);
  color: var(--siq-text-tertiary);
  font:
    700 8px/1.35 var(--font-mp-mono, 'Diatype Mono'),
    ui-monospace,
    monospace;
  text-transform: uppercase;
}
.match p {
  margin: var(--siq-space-2) 0 0;
  color: var(--siq-text-secondary);
  font-size: 10px;
  line-height: 1.5;
}
.matchGap {
  padding: var(--siq-space-3);
  border: 1px dashed var(--siq-line-strong);
  border-radius: var(--siq-radius-card);
  background: var(--siq-surface-card);
}
.matchGap p {
  margin: 0;
  color: var(--siq-muted);
  font-size: 10px;
  line-height: 1.5;
}
.evidence {
  display: block;
  margin-top: var(--siq-space-2);
  padding: var(--siq-space-3);
  border: 1px solid var(--siq-border);
  border-radius: var(--siq-radius-card);
  background: var(--siq-surface-card);
  color: inherit;
  text-decoration: none;
  transition:
    background-color var(--siq-fast) var(--siq-ease-feedback),
    border-color var(--siq-fast) var(--siq-ease-feedback),
    transform var(--siq-fast) var(--siq-ease-feedback);
}
.evidence:hover {
  transform: translateY(-1px);
  border-color: var(--siq-border-strong);
  background: var(--siq-surface-card-hover);
}
.evidence > div {
  display: flex;
  justify-content: space-between;
  gap: var(--siq-space-2);
}
.evidence span,
.evidence em {
  color: var(--siq-accent-primary);
  font:
    700 8px/1 var(--font-mp-mono, 'Diatype Mono'),
    ui-monospace,
    monospace;
  text-transform: uppercase;
  font-style: normal;
}
.evidence p {
  margin: var(--siq-space-2) 0;
  color: var(--siq-text-secondary);
  font-size: 10px;
  line-height: 1.5;
}
.evidence small {
  color: var(--siq-muted);
  font-size: 9px;
}
.gate {
  min-height: 46px;
  display: grid;
  grid-template-columns: 28px minmax(0, 1fr);
  gap: var(--siq-space-2);
  align-items: center;
  padding: var(--siq-space-2) 0;
  border-top: 1px solid var(--siq-border);
}
.gate > span {
  width: 22px;
  height: 22px;
  display: grid;
  place-items: center;
  border: 1px solid var(--siq-border-strong);
  border-radius: var(--siq-radius-control);
}
.gate > span svg {
  width: 11px;
}
.gate > span i {
  width: 5px;
  height: 5px;
  border-radius: 50%;
  background: var(--siq-amber);
}
.gate > span.gatePass {
  background: var(--siq-forest);
  color: var(--siq-lime);
  border-color: var(--siq-forest);
}
.gate div {
  display: grid;
  gap: 3px;
}
.gate strong {
  font-size: 10px;
}
.gate small {
  color: var(--siq-muted);
  font-size: 9px;
}
.decisionRow {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 2px;
  margin-bottom: var(--siq-space-3);
  padding: 3px;
  border: 1px solid var(--siq-border);
  border-radius: var(--siq-radius-card);
  background: rgba(72, 107, 87, 0.06);
}
.decisionRow button {
  height: var(--siq-control-compact);
  border: 0;
  border-radius: var(--siq-radius-control);
  background: transparent;
  color: var(--siq-muted);
  font:
    700 8px/1 var(--font-mp-sans, 'DM Sans'),
    sans-serif;
  text-transform: uppercase;
  cursor: pointer;
}
.decisionRow button:hover {
  background: var(--siq-surface-card-hover);
  color: var(--siq-ink);
}
.decisionRow .decisionActive {
  background: var(--siq-forest);
  color: var(--siq-lime);
  box-shadow: var(--siq-shadow-1);
}
.controlLabel {
  display: grid;
  gap: 6px;
  margin: var(--siq-space-3) 0;
  color: var(--siq-muted);
  font-size: 9px;
  text-transform: uppercase;
}
.controlLabel select {
  height: var(--siq-control);
  padding: 0 var(--siq-space-2);
  border: 1px solid var(--siq-border-strong);
  border-radius: var(--siq-radius-control);
  background: var(--siq-surface-input);
  color: var(--siq-ink);
  font-size: 10px;
}
.flagButton {
  width: 100%;
  height: var(--siq-control);
  display: flex;
  align-items: center;
  justify-content: center;
  gap: var(--siq-space-2);
  border: 1px solid var(--siq-border-strong);
  border-radius: var(--siq-radius-control);
  background: var(--siq-surface-input);
  color: var(--siq-ink);
  box-shadow: var(--siq-shadow-1);
  font:
    700 9px/1 var(--font-mp-sans, 'DM Sans'),
    sans-serif;
  cursor: pointer;
  transition:
    transform var(--siq-fast) var(--siq-ease-feedback),
    box-shadow var(--siq-fast) var(--siq-ease-feedback);
}
.flagButton:hover {
  transform: translateY(-1px);
  box-shadow: var(--siq-shadow-2);
}
.flagButton svg {
  width: 13px;
}
.commentForm {
  position: relative;
  margin-top: var(--siq-space-4);
}
.commentForm textarea {
  width: 100%;
  min-height: 88px;
  resize: vertical;
  padding: var(--siq-space-3) 46px var(--siq-space-3) var(--siq-space-3);
  border: 1px solid var(--siq-border-strong);
  border-radius: var(--siq-radius-card);
  background: var(--siq-surface-input);
  color: var(--siq-ink);
  font:
    500 10px/1.5 var(--font-mp-sans, 'DM Sans'),
    sans-serif;
}
.commentForm button {
  position: absolute;
  right: var(--siq-space-2);
  bottom: var(--siq-space-2);
  width: var(--siq-control-compact);
  height: var(--siq-control-compact);
  display: grid;
  place-items: center;
  border: 0;
  border-radius: var(--siq-radius-control);
  background: var(--siq-forest);
  color: var(--siq-lime);
  cursor: pointer;
}
.commentForm button svg {
  width: 12px;
}
.readOnlyNotice {
  margin-bottom: var(--siq-space-4);
  padding: var(--siq-space-4);
  border: 1px solid var(--siq-border);
  border-radius: var(--siq-radius-card);
  background: var(--siq-surface-muted);
}
.readOnlyNotice strong {
  font-size: 11px;
}
.readOnlyNotice p {
  margin: var(--siq-space-2) 0 0;
  color: var(--siq-muted);
  font-size: 10px;
  line-height: 1.5;
}
.activity {
  display: grid;
  grid-template-columns: 30px minmax(0, 1fr);
  gap: var(--siq-space-2);
  padding: var(--siq-space-3) 0;
  border-top: 1px solid var(--siq-border);
}
.activity > span {
  width: 28px;
  height: 28px;
  display: grid;
  place-items: center;
  border-radius: var(--siq-radius-control);
  background: var(--siq-surface-card);
  font:
    700 8px/1 var(--font-mp-mono, 'Diatype Mono'),
    ui-monospace,
    monospace;
}
.activity div {
  min-width: 0;
}
.activity strong {
  font-size: 10px;
}
.activity p {
  margin: var(--siq-space-1) 0;
  color: var(--siq-text-secondary);
  font-size: 10px;
  line-height: 1.45;
}
.activity small {
  color: var(--siq-muted);
  font-size: 8px;
}

.practiceView,
.teamView,
.billingView {
  min-height: calc(100vh - 64px);
  min-height: calc(100dvh - 64px);
  padding-bottom: var(--siq-space-6);
  background: transparent;
}
.pageIntro {
  position: relative;
  min-height: 210px;
  display: flex;
  justify-content: space-between;
  align-items: end;
  gap: var(--siq-space-6);
  margin: var(--siq-space-3) var(--siq-space-3) 0;
  padding: var(--siq-space-6) clamp(20px, 5vw, 70px);
  overflow: hidden;
  border: 1px solid var(--siq-line-default);
  border-radius: var(--siq-radius-panel);
  background: linear-gradient(110deg, #101718 0%, #111d18 48%, #121516 100%);
  color: var(--siq-field-text);
  box-shadow: var(--siq-shadow-2);
}
.pageIntro > *:not(:first-child),
.introCopy {
  position: relative;
  z-index: 2;
}
.pageIntro .kicker {
  color: var(--siq-spectral-ice);
}
.pageIntro p {
  color: var(--siq-field-muted);
}
.pageIntro p {
  margin: var(--siq-space-2) 0 0;
  color: var(--siq-muted);
  font-size: 12px;
}
.pageStat {
  display: grid;
  text-align: right;
  text-shadow: 0 2px 20px rgba(0, 0, 0, 0.42);
}
.pageStat strong {
  font:
    750 34px/1 var(--font-mp-mono, 'Diatype Mono'),
    ui-monospace,
    monospace;
}
.pageStat span {
  color: var(--siq-muted);
  font-size: 9px;
  text-transform: uppercase;
}
.practiceFilters {
  min-height: 54px;
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: var(--siq-space-3);
  max-width: 1380px;
  margin: var(--siq-space-3) auto 0;
  padding: var(--siq-space-2) var(--siq-space-3);
  border: 1px solid var(--siq-line-default);
  border-radius: var(--siq-radius-panel) var(--siq-radius-panel) 0 0;
  background: var(--siq-surface);
  box-shadow: var(--siq-shadow-1);
  backdrop-filter: blur(18px);
}
.practiceFilters > span {
  margin-right: auto;
  color: var(--siq-muted);
  font:
    700 9px/1 var(--font-mp-mono, 'Diatype Mono'),
    ui-monospace,
    monospace;
  text-transform: uppercase;
}
.practiceFilters label {
  display: flex;
  align-items: center;
  gap: var(--siq-space-2);
  color: var(--siq-muted);
  font-size: 9px;
  text-transform: uppercase;
}
.practiceFilters select {
  max-width: 180px;
  height: var(--siq-control-compact);
  border: 1px solid var(--siq-border-strong);
  border-radius: var(--siq-radius-control);
  background: var(--siq-surface-input);
  color: var(--siq-ink);
  font-size: 10px;
}
.practiceGrid {
  display: grid;
  grid-template-columns: minmax(0, 1fr) minmax(320px, 420px);
  gap: var(--siq-space-3);
  max-width: 1380px;
  margin: 0 auto;
  padding: var(--siq-space-3);
  border: 1px solid var(--siq-line-default);
  border-top: 0;
  border-radius: 0 0 var(--siq-radius-panel) var(--siq-radius-panel);
  background: var(--siq-surface-rail);
  box-shadow: var(--siq-shadow-2);
}
.precedentRegister {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  align-content: start;
  gap: var(--siq-space-3);
}
.precedentRegister article {
  min-height: 210px;
  padding: var(--siq-space-4);
  border: 1px solid var(--siq-border);
  border-radius: var(--siq-radius-card);
  background: var(--siq-surface-strong);
  box-shadow: var(--siq-shadow-1);
  overflow: hidden;
  transition:
    transform var(--siq-select) var(--siq-ease-select),
    box-shadow var(--siq-select) var(--siq-ease-select),
    border-color var(--siq-fast) var(--siq-ease-feedback);
}
.precedentRegister article:hover {
  transform: translateY(-2px);
  border-color: var(--siq-border-strong);
  box-shadow: var(--siq-shadow-2);
}
.precedentRegister article > img {
  width: calc(100% + 32px);
  aspect-ratio: 16 / 8;
  object-fit: cover;
  display: block;
  margin: -16px -16px var(--siq-space-4);
  filter: saturate(0.82) contrast(1.03);
  transition:
    transform var(--siq-relate) var(--siq-ease-relate),
    filter var(--siq-relate) var(--siq-ease-relate);
}
.precedentRegister article:hover > img {
  transform: scale(1.012);
  filter: saturate(1) contrast(1.02);
}
.precedentRegister article > div {
  display: flex;
  justify-content: space-between;
  gap: var(--siq-space-2);
}
.precedentRegister article span,
.precedentRegister article em {
  color: var(--siq-accent-primary);
  font:
    700 8px/1 var(--font-mp-mono, 'Diatype Mono'),
    ui-monospace,
    monospace;
  text-transform: uppercase;
  font-style: normal;
}
.precedentRegister h2 {
  margin: var(--siq-space-4) 0 var(--siq-space-2);
  font-size: 16px;
}
.precedentRegister p {
  color: var(--siq-text-secondary);
  font-size: 11px;
  line-height: 1.55;
}
.precedentRegister small {
  color: var(--siq-muted);
  font-size: 9px;
}
.practiceForm {
  align-self: start;
  position: sticky;
  top: var(--siq-space-3);
  display: grid;
  gap: var(--siq-space-3);
  padding: var(--siq-space-5);
  border: 1px solid rgba(176, 207, 188, 0.16);
  border-radius: var(--siq-radius-panel);
  background: rgba(10, 23, 16, 0.95);
  color: #fff;
  box-shadow: var(--siq-shadow-2);
}
.practiceForm h2 {
  margin: 0;
  font-size: 19px;
}
.practiceForm label {
  display: grid;
  gap: 6px;
  color: #9caf9f;
  font-size: 9px;
  text-transform: uppercase;
}
.practiceForm input,
.practiceForm textarea {
  width: 100%;
  min-width: 0;
  padding: var(--siq-space-3);
  border: 1px solid #426051;
  border-radius: var(--siq-radius-control);
  background: #0e2017;
  color: #fff;
  font:
    500 10px/1.5 var(--font-mp-sans, 'DM Sans'),
    sans-serif;
}
.practiceForm textarea {
  min-height: 96px;
  resize: vertical;
}
.practiceForm > div {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: var(--siq-space-2);
}
.practiceForm button,
.inviteForm button {
  height: var(--siq-control);
  display: flex;
  justify-content: center;
  align-items: center;
  gap: var(--siq-space-2);
  border: 0;
  border-radius: var(--siq-radius-control);
  background: var(--siq-lime);
  color: var(--siq-ink);
  box-shadow: var(--siq-shadow-1);
  font:
    750 9px/1 var(--font-mp-sans, 'DM Sans'),
    sans-serif;
  cursor: pointer;
  transition:
    transform var(--siq-fast) var(--siq-ease-feedback),
    box-shadow var(--siq-fast) var(--siq-ease-feedback);
}
.practiceForm button:hover,
.inviteForm button:hover {
  transform: translateY(-1px);
  box-shadow: var(--siq-shadow-2);
}
.practiceForm button svg {
  width: 13px;
}
.inviteForm {
  display: grid;
  grid-template-columns: 28px minmax(180px, 1fr) 94px 78px;
  align-items: center;
  overflow: hidden;
  border: 1px solid var(--siq-border-strong);
  border-radius: var(--siq-radius-card);
  background: var(--siq-surface-strong);
  box-shadow: var(--siq-shadow-1);
}
.inviteForm svg {
  width: 14px;
  margin-left: var(--siq-space-3);
}
.inviteForm input {
  height: var(--siq-control);
  min-width: 0;
  border: 0;
  outline: 0;
  background: transparent;
  font:
    500 10px/1 var(--font-mp-sans, 'DM Sans'),
    sans-serif;
}
.inviteForm select {
  height: var(--siq-control);
  min-width: 0;
  border: 0;
  border-left: 1px solid var(--siq-border);
  padding: 0 var(--siq-space-2);
  background: var(--siq-surface-input);
  color: var(--siq-ink);
  font-size: 9px;
  text-transform: uppercase;
}
.inviteForm button {
  border-radius: 0;
}
.teamRegister {
  max-width: 1100px;
  margin: var(--siq-space-5) auto;
  padding: var(--siq-space-2);
  border: 1px solid var(--siq-line-default);
  border-radius: var(--siq-radius-panel);
  background: var(--siq-surface);
  box-shadow: var(--siq-shadow-2);
  backdrop-filter: blur(18px);
}
.teamRegister article {
  display: grid;
  grid-template-columns: 40px minmax(0, 1fr) auto;
  gap: var(--siq-space-3);
  align-items: center;
  min-height: 68px;
  margin-top: var(--siq-space-1);
  padding: 0 var(--siq-space-4);
  border: 1px solid transparent;
  border-radius: var(--siq-radius-card);
  background: var(--siq-surface-card);
  transition:
    border-color var(--siq-fast) var(--siq-ease-feedback),
    background-color var(--siq-fast) var(--siq-ease-feedback),
    box-shadow var(--siq-fast) var(--siq-ease-feedback);
}
.teamRegister article:hover {
  border-color: var(--siq-border);
  background: var(--siq-surface-card-hover);
  box-shadow: var(--siq-shadow-1);
}
.teamRegister article > span {
  width: 32px;
  height: 32px;
  display: grid;
  place-items: center;
  border-radius: var(--siq-radius-control);
  background: var(--siq-forest);
  color: var(--siq-lime);
  font:
    700 8px/1 var(--font-mp-mono, 'Diatype Mono'),
    ui-monospace,
    monospace;
}
.teamRegister div {
  min-width: 0;
  display: grid;
  gap: 3px;
}
.teamRegister strong {
  font-size: 12px;
}
.teamRegister small {
  overflow: hidden;
  text-overflow: ellipsis;
  color: var(--siq-muted);
  font-size: 10px;
}
.teamRegister em {
  color: var(--siq-accent-primary);
  font:
    700 8px/1 var(--font-mp-mono, 'Diatype Mono'),
    ui-monospace,
    monospace;
  text-transform: uppercase;
  font-style: normal;
}
.emptyState {
  padding: 40px 20px;
  color: var(--siq-muted);
  font-size: 12px;
}

.billingGrid {
  display: grid;
  grid-template-columns: minmax(0, 1.35fr) minmax(320px, 0.65fr);
  gap: var(--siq-space-3);
  max-width: 1180px;
  margin: var(--siq-space-4) auto;
  padding: 0 var(--siq-space-3);
}
.billingSummary,
.billingForm {
  border: 1px solid var(--siq-line-default);
  border-radius: var(--siq-radius-panel);
  background: var(--siq-surface);
  box-shadow: var(--siq-shadow-2);
  backdrop-filter: blur(18px);
}
.billingSummary {
  padding: var(--siq-space-5);
}
.billingHeading {
  display: grid;
  grid-template-columns: 40px minmax(0, 1fr) auto;
  align-items: center;
  gap: var(--siq-space-3);
  padding-bottom: var(--siq-space-5);
  border-bottom: 1px solid var(--siq-border);
}
.billingHeading > svg {
  width: 18px;
  color: var(--siq-accent-primary);
}
.billingHeading span,
.billingMetrics span,
.agreementLine > span {
  color: var(--siq-muted);
  font:
    700 8px/1 var(--font-mp-mono, 'Diatype Mono'),
    ui-monospace,
    monospace;
  text-transform: uppercase;
}
.billingHeading h2 {
  margin: 5px 0 0;
  font-size: 22px;
}
.billingHeading > strong {
  color: var(--siq-lime);
  font:
    750 18px/1 var(--font-mp-mono, 'Diatype Mono'),
    ui-monospace,
    monospace;
}
.billingMetrics {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  margin: var(--siq-space-4) 0;
  border-block: 1px solid var(--siq-border);
}
.billingMetrics > div {
  min-height: 84px;
  display: grid;
  align-content: space-between;
  gap: var(--siq-space-3);
  padding: var(--siq-space-3) var(--siq-space-4);
}
.billingMetrics > div + div {
  border-left: 1px solid var(--siq-border);
}
.billingMetrics strong {
  font-size: 12px;
  line-height: 1.35;
}
.creditLine {
  display: grid;
  grid-template-columns: 32px minmax(0, 1fr) auto;
  align-items: center;
  gap: var(--siq-space-3);
  padding: var(--siq-space-4);
  border: 1px solid color-mix(in srgb, var(--siq-lime) 42%, var(--siq-border));
  border-radius: var(--siq-radius-card);
  background: color-mix(in srgb, var(--siq-lime) 7%, var(--siq-surface-card));
}
.creditLine > span {
  width: 28px;
  height: 28px;
  display: grid;
  place-items: center;
  border-radius: var(--siq-radius-control);
  background: var(--siq-lime);
  color: var(--siq-ink);
}
.creditLine svg {
  width: 13px;
}
.creditLine div {
  display: grid;
  gap: 4px;
}
.creditLine small {
  color: var(--siq-muted);
  font-size: 9px;
}
.creditLine em {
  color: var(--siq-accent-primary);
  font:
    700 9px/1 var(--font-mp-mono, 'Diatype Mono'),
    ui-monospace,
    monospace;
  font-style: normal;
  text-transform: uppercase;
}
.agreementLine {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  gap: 7px var(--siq-space-3);
  margin-top: var(--siq-space-4);
  padding: var(--siq-space-3) 0;
  border-top: 1px solid var(--siq-border);
}
.agreementLine small {
  color: var(--siq-muted);
  font-size: 9px;
}
.agreementLine a {
  color: var(--siq-accent-primary);
  font-size: 9px;
}
.proposalButton,
.billingForm button {
  min-height: var(--siq-control);
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: var(--siq-space-2);
  border: 0;
  border-radius: var(--siq-radius-control);
  padding: 0 var(--siq-space-4);
  background: var(--siq-lime);
  color: var(--siq-ink);
  font:
    750 9px/1 var(--font-mp-sans, 'DM Sans'),
    sans-serif;
  cursor: pointer;
}
.proposalButton svg {
  width: 13px;
}
.proposalButton:disabled,
.billingForm button:disabled {
  opacity: 0.55;
  cursor: default;
}
.billingForm {
  align-self: start;
  display: grid;
  gap: var(--siq-space-3);
  padding: var(--siq-space-5);
}
.billingForm h2 {
  margin: 0;
  font-size: 19px;
}
.billingForm label {
  display: grid;
  gap: 6px;
  color: var(--siq-muted);
  font-size: 9px;
  text-transform: uppercase;
}
.billingForm input {
  width: 100%;
  height: var(--siq-control);
  border: 1px solid var(--siq-border-strong);
  border-radius: var(--siq-radius-control);
  padding: 0 var(--siq-space-3);
  background: var(--siq-surface-input);
  color: var(--siq-ink);
  font-size: 10px;
}
.billingForm > small {
  color: var(--siq-muted);
  font-size: 9px;
  line-height: 1.5;
}

@keyframes spin {
  to {
    transform: rotate(360deg);
  }
}
@keyframes pulse {
  to {
    opacity: 0.25;
    transform: scale(0.65);
  }
}
@keyframes rise {
  from {
    opacity: 0;
    transform: translateY(6px);
  }
}
@keyframes panelReveal {
  from {
    opacity: 0;
    transform: translateX(8px);
  }
}
@keyframes signalArrive {
  from {
    opacity: 0;
    transform: translateY(8px) scale(0.99);
  }
}

@media (max-width: 1179px) {
  .header {
    grid-template-columns: minmax(0, 1fr) auto;
  }
  .cockpit {
    position: relative;
    grid-template-columns: minmax(280px, 34vw) minmax(0, 1fr);
    gap: var(--siq-space-3);
  }
  .signalRail {
    grid-column: 1;
    grid-row: 1;
  }
  .field {
    grid-column: 2;
    grid-row: 1;
  }
  .intelligenceRail {
    position: absolute;
    z-index: 8;
    top: var(--siq-space-3);
    right: var(--siq-space-3);
    bottom: var(--siq-space-3);
    width: min(430px, calc(100% - 48px));
    border: 1px solid var(--siq-line-default);
    box-shadow: var(--siq-shadow-3);
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
    transform: translateX(calc(100% + var(--siq-space-5)));
    transition:
      transform var(--siq-relate) var(--siq-ease-relate),
      opacity var(--siq-select) var(--siq-ease-feedback),
      visibility 0s linear var(--siq-relate);
  }
  .intelligenceRail.inspectorOpen {
    opacity: 1;
    visibility: visible;
    pointer-events: auto;
    transform: translateX(0);
    transition-delay: 0s;
  }
  .inspectorClose {
    position: absolute;
    z-index: 4;
    top: 6px;
    right: 6px;
    width: var(--siq-control-compact);
    height: var(--siq-control-compact);
    display: grid;
    place-items: center;
    border: 1px solid var(--siq-border);
    border-radius: var(--siq-radius-control);
    background: var(--siq-surface-strong);
    color: var(--siq-ink);
    box-shadow: var(--siq-shadow-1);
    cursor: pointer;
  }
  .inspectorClose svg {
    width: 15px;
  }
  .inspectorTabs {
    padding-right: 42px;
  }
  .inspectorBackdrop {
    position: absolute;
    z-index: 7;
    inset: 0;
    display: block;
    border: 0;
    background: rgba(7, 17, 12, 0.28);
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
    backdrop-filter: blur(2px);
    cursor: default;
    transition:
      opacity var(--siq-select) var(--siq-ease-feedback),
      visibility 0s linear var(--siq-select);
  }
  .inspectorBackdrop.inspectorBackdropOpen {
    opacity: 1;
    visibility: visible;
    pointer-events: auto;
    transition-delay: 0s;
  }
  .fieldMetrics span:nth-child(3) {
    display: none;
  }
}

@media (max-width: 767px) {
  .shell {
    min-height: 100dvh;
    overflow: visible;
  }
  .header {
    position: sticky;
    top: 0;
    height: 64px;
    min-height: 64px;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: var(--siq-space-2);
    padding: var(--siq-space-2);
  }
  .headerActions {
    position: static;
  }
  .identity,
  .menuToggle,
  .themeToggle {
    width: 40px;
    height: 40px;
    flex-basis: 40px;
  }
  .brand {
    height: var(--siq-control);
    padding: 0 var(--siq-space-1);
  }
  .header nav {
    justify-self: stretch;
    width: 100%;
    height: var(--siq-control-touch);
    overflow-x: auto;
    overscroll-behavior-x: contain;
    scrollbar-width: none;
  }
  .header nav::-webkit-scrollbar {
    display: none;
  }
  .header nav button {
    min-height: var(--siq-control);
    flex: 0 0 auto;
    padding: 0 var(--siq-space-3);
  }
  .cockpit {
    height: auto;
    min-height: calc(100dvh - 104px);
    display: flex;
    flex-direction: column;
    gap: var(--siq-space-3);
    padding: var(--siq-space-3);
    overflow: visible;
  }
  .signalRail,
  .field,
  .intelligenceRail {
    width: 100%;
    height: auto;
    max-height: none;
    flex: none;
  }
  .field {
    order: 1;
    min-height: clamp(340px, 56dvh, 460px);
    grid-template-rows: 56px minmax(240px, 1fr) auto 32px;
  }
  .signalRail {
    order: 2;
    display: block;
  }
  .intelligenceRail {
    position: fixed;
    z-index: 21;
    inset: 104px 0 0;
    width: 100%;
    max-height: calc(100dvh - 104px);
    display: grid;
    grid-template-rows: auto auto minmax(0, 1fr);
    border-radius: var(--siq-radius-panel) var(--siq-radius-panel) 0 0;
    transform: translateY(100%);
  }
  .intelligenceRail.inspectorOpen {
    transform: translateY(0);
  }
  .inspectorBackdrop {
    position: fixed;
    z-index: 20;
    inset: 104px 0 0;
  }
  .signals {
    overflow: visible;
    scrollbar-gutter: auto;
  }
  .inspectorBody {
    overflow-x: hidden;
    overflow-y: auto;
    overscroll-behavior: contain;
    scrollbar-gutter: stable;
  }
  .signals {
    padding-bottom: var(--siq-space-3);
  }
  .inspectorTabs {
    position: relative;
    top: auto;
    z-index: 3;
    background: var(--siq-surface-rail);
    backdrop-filter: blur(18px);
  }
  .inspectorTitle {
    position: relative;
    top: auto;
  }
  .fieldMetrics {
    display: none;
  }
  .fieldSelection {
    min-height: 72px;
  }
  .signals > button {
    min-height: 108px;
  }
  .sonar {
    width: min(78%, 360px);
  }
  .sonar button {
    width: 44px;
    height: 44px;
    border-width: 18px;
  }
  .sonar button.activeSignal {
    width: 44px;
    height: 44px;
  }
  .modeSwitch button,
  .fieldSelection button,
  .railHead > button,
  .inspectorTabs button,
  .decisionRow button,
  .controlLabel select,
  .flagButton,
  .commentForm button,
  .practiceFilters select,
  .practiceForm button,
  .inviteForm button {
    min-height: 44px;
  }
  .railHead > button,
  .fieldSelection button,
  .commentForm button {
    width: 44px;
    height: 44px;
  }
  .practiceView,
  .teamView,
  .billingView {
    min-height: calc(100dvh - 64px);
  }
  .pageIntro {
    align-items: start;
    flex-direction: column;
    min-height: 140px;
  }
  .practiceFilters {
    margin-inline: var(--siq-space-3);
    flex-wrap: wrap;
    justify-content: flex-start;
  }
  .practiceFilters > span {
    width: 100%;
    margin-right: 0;
  }
  .practiceGrid {
    grid-template-columns: minmax(0, 1fr);
    margin-inline: var(--siq-space-3);
  }
  .precedentRegister {
    grid-template-columns: minmax(0, 1fr);
  }
  .practiceForm {
    position: static;
  }
  .inviteForm {
    width: 100%;
    grid-template-columns: 28px minmax(0, 1fr) 88px 76px;
  }
  .billingGrid {
    grid-template-columns: minmax(0, 1fr);
  }
  .teamRegister {
    margin: var(--siq-space-4);
  }
  .error {
    top: 76px;
    width: calc(100vw - 24px);
  }
}

@media (max-width: 480px) {
  .brand small {
    font-size: 10px;
  }
  .header nav button {
    padding: 0 10px;
    font-size: 10px;
  }
  .cockpit {
    padding: var(--siq-space-2);
    gap: var(--siq-space-2);
  }
  .railHead {
    padding: var(--siq-space-4) var(--siq-space-3) var(--siq-space-3);
  }
  .railHead h1,
  .pageIntro h1 {
    font-size: 21px;
  }
  .search {
    margin: 0 var(--siq-space-3) var(--siq-space-2);
  }
  .signalCount {
    margin-inline: var(--siq-space-3);
  }
  .signals {
    padding-inline: var(--siq-space-2);
  }
  .signals > button {
    grid-template-columns: 24px minmax(0, 1fr) 38px;
  }
  .fieldTop {
    padding: 0 var(--siq-space-2);
  }
  .modeSwitch button {
    padding: 0 var(--siq-space-2);
  }
  .fieldSelection {
    grid-template-columns: 46px minmax(0, 1fr) 44px;
    margin: 0 var(--siq-space-2) var(--siq-space-2);
    padding: var(--siq-space-2);
  }
  .fieldStatus {
    padding: 0 var(--siq-space-2);
  }
  .fieldStatus span {
    font-size: 7px;
  }
  .inspectorTitle,
  .inspectorBody {
    padding-left: var(--siq-space-3);
    padding-right: var(--siq-space-3);
  }
  .inspectorBody dl {
    grid-template-columns: minmax(0, 1fr);
  }
  .pageIntro {
    gap: var(--siq-space-4);
    margin: var(--siq-space-2);
    padding: var(--siq-space-5) var(--siq-space-4);
  }
  .practiceFilters,
  .practiceGrid {
    margin-inline: var(--siq-space-2);
  }
  .practiceForm {
    padding: var(--siq-space-4);
  }
  .practiceFilters label {
    width: 100%;
    justify-content: space-between;
  }
  .practiceFilters select {
    flex: 1;
    max-width: none;
  }
  .practiceForm > div {
    grid-template-columns: minmax(0, 1fr);
  }
  .inviteForm {
    grid-template-columns: 28px minmax(0, 1fr) 72px;
  }
  .inviteForm select {
    grid-column: 2 / 3;
    border-top: 1px solid var(--siq-border);
    border-left: 0;
  }
  .inviteForm button {
    grid-column: 3;
    grid-row: 1 / 3;
    height: 100%;
  }
  .billingMetrics {
    grid-template-columns: minmax(0, 1fr);
  }
  .billingMetrics > div + div {
    border-top: 1px solid var(--siq-border);
    border-left: 0;
  }
  .billingHeading {
    grid-template-columns: 32px minmax(0, 1fr);
  }
  .billingHeading > strong {
    grid-column: 2;
  }
  .teamRegister {
    margin: var(--siq-space-2);
  }
  .teamRegister article {
    grid-template-columns: 36px minmax(0, 1fr) auto;
    padding: 0 var(--siq-space-3);
  }
}

@supports not (
  (backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))
) {
  .header,
  .signalRail,
  .intelligenceRail,
  .inspectorTitle,
  .pageIntro,
  .practiceFilters,
  .teamRegister {
    background: var(--siq-surface-raised);
  }
  .fieldTop,
  .fieldSelection {
    background: var(--siq-field-raised);
  }
  .inspectorBackdrop {
    background: var(--siq-surface-overlay);
  }
}

@media (prefers-contrast: more) {
  .shell {
    --siq-surface-rail: #111713;
    --siq-surface-raised: #171e1a;
    --siq-surface-card: #1c241f;
    --siq-surface-card-hover: #263129;
    --siq-surface-selected: #304235;
    --siq-line-subtle: rgba(241, 245, 242, 0.22);
    --siq-line-default: rgba(241, 245, 242, 0.38);
    --siq-line-strong: rgba(241, 245, 242, 0.62);
  }
  :global(:root[data-theme='light']) .shell {
    --siq-surface-rail: #ffffff;
    --siq-surface-raised: #ffffff;
    --siq-surface-card: #f5f7f4;
    --siq-surface-card-hover: #ffffff;
    --siq-surface-selected: #e4eddf;
    --siq-line-subtle: rgba(7, 17, 12, 0.18);
    --siq-line-default: rgba(7, 17, 12, 0.32);
    --siq-line-strong: rgba(7, 17, 12, 0.56);
  }
  .signalRail,
  .intelligenceRail,
  .pageIntro,
  .practiceFilters,
  .practiceGrid,
  .teamRegister {
    backdrop-filter: none;
  }
  .selectedSignal,
  .workspaceLink,
  .inspectorActive {
    border-width: 2px;
  }
}

@media (prefers-reduced-motion: reduce) {
  .shell *,
  .shell *::before,
  .shell *::after {
    scroll-behavior: auto !important;
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    transition-delay: 0ms !important;
  }
  .sonarSweep,
  .loader i,
  .fieldSelection,
  .inspectorBody,
  .signals > button {
    animation: none !important;
  }
  .signals > button:hover,
  .signals > button.selectedSignal,
  .match:hover,
  .evidence:hover,
  .precedentRegister article:hover,
  .railHead > button:hover,
  .flagButton:hover,
  .practiceForm button:hover,
  .inviteForm button:hover {
    transform: none;
  }
}

.agentLauncher{display:inline-flex;align-items:center;gap:8px;padding:9px 12px;border:1px solid var(--siq-line-strong);border-radius:5px;background:var(--siq-surface-card);color:var(--siq-text-primary);font:12px var(--font-mp-sans,Arial),sans-serif;cursor:pointer}.agentLauncher:hover{border-color:var(--siq-accent-focus)}.agentLauncher span{color:var(--siq-accent-focus)}.agentBackdrop{position:fixed;inset:0;z-index:119;background:rgba(2,7,4,.38);border:0;cursor:default}.agentDrawer{position:fixed;right:0;top:0;bottom:0;width:min(420px,100vw);z-index:120;animation:agentEnter .22s ease-out}@keyframes agentEnter{from{transform:translateX(24px);opacity:.5}to{transform:translateX(0);opacity:1}}@media(prefers-reduced-motion:reduce){.agentDrawer{animation:none}}
.agentDrawerExpanded{width:min(900px,100vw)}
.brand > .brandMark{display:block;width:30px;height:29px;flex:0 0 30px;border-radius:0;background:var(--siq-field-text);box-shadow:none;-webkit-mask:url('/images/White.svg') center / contain no-repeat;mask:url('/images/White.svg') center / contain no-repeat}
.brandByline{display:flex;align-items:center;gap:5px;line-height:1}
.brandWatermark{display:inline-block;width:82px;height:12px;flex:none;background:var(--siq-field-muted);-webkit-mask:url('/images/mirror-progress-wordmark.svg') left center / contain no-repeat;mask:url('/images/mirror-progress-wordmark.svg') left center / contain no-repeat}
.agentLauncher .agentMark,.agentMinimized .agentMark{display:inline-block;width:17px;height:16px;flex:none;margin-left:2px;background:var(--siq-accent-focus);vertical-align:-3px;-webkit-mask:url('/images/White.svg') center / contain no-repeat;mask:url('/images/White.svg') center / contain no-repeat}
.workspaceLayout .agentDock{border-color:rgba(220,239,225,.16);border-radius:14px;box-shadow:0 18px 54px rgba(0,0,0,.32),0 1px 0 rgba(255,255,255,.04)}
.workspaceLayout .agentDockExpanded{border-radius:10px;box-shadow:none}
.workspaceLayoutExpanded{grid-template-columns:minmax(0,1fr) 12px var(--prospect-chat-width)!important;gap:0!important}
.workspaceResize{position:relative;align-self:stretch;min-height:calc(100dvh - 76px);cursor:col-resize;touch-action:none;z-index:2}
.workspaceResize::before{content:'';position:absolute;left:5px;top:12px;bottom:12px;width:2px;border-radius:2px;background:var(--siq-line-strong);transition:background .15s}
.workspaceResize:hover::before,.workspaceResize:focus-visible::before{background:var(--siq-accent-focus)}
.workspaceResize:focus-visible{outline:2px solid var(--siq-accent-focus);outline-offset:-2px}
@media(max-width:900px){.workspaceLayoutExpanded{display:block}.workspaceResize{display:none}}
.workspaceLayout{position:relative;min-width:0}.workspaceMain{min-width:0}.agentDock{position:fixed;right:18px;bottom:18px;z-index:75;width:min(360px,calc(100vw - 36px));height:min(480px,calc(100dvh - 100px));border:1px solid var(--siq-line-strong);border-radius:10px;overflow:hidden;box-shadow:0 20px 70px rgba(0,0,0,.48)}.workspaceLayoutExpanded{display:grid;grid-template-columns:minmax(0,1fr) minmax(390px,min(42vw,650px));align-items:start;gap:12px}.agentDockExpanded{position:sticky;top:74px;right:auto;bottom:auto;width:auto;height:calc(100dvh - 86px);min-height:600px;z-index:1;border-radius:8px;box-shadow:none}.agentMinimized{position:fixed;right:18px;bottom:18px;z-index:75;border:1px solid var(--siq-line-strong);border-radius:7px;background:var(--siq-field-raised);color:var(--siq-text-primary);padding:12px 16px;box-shadow:var(--siq-shadow-mid);font:12px var(--font-mp-sans,Arial),sans-serif;cursor:pointer}.agentMinimized span{color:var(--siq-accent-focus);margin-left:8px}@media(max-width:900px){.workspaceLayoutExpanded{display:block}.agentDockExpanded{position:fixed;top:72px;bottom:0;right:0;left:0;width:auto;height:auto;min-height:0;z-index:100;border:0;border-radius:0}.agentDock{height:min(440px,calc(100dvh - 100px))}}@media(max-width:500px){.agentDock{right:10px;bottom:10px;width:calc(100vw - 20px);height:min(420px,calc(100dvh - 86px))}.agentDockExpanded{top:63px;right:0;bottom:0;width:100vw;height:auto}}

/* Precision rail: one quiet surface, four clear targets, one active signal. */
.shell {
  --control-rail: rgba(16, 21, 22, .92);
  --control-rail-edge: rgba(213, 229, 221, .23);
  --control-divider: rgba(213, 229, 221, .18);
  --control-hover: rgba(222, 240, 225, .075);
  --control-ink: #e8eeea;
  --control-mark: #a8e89a;
  --control-shadow: 0 8px 24px rgba(0, 0, 0, .2), inset 0 1px rgba(255, 255, 255, .035);
}
:global(:root[data-theme='light']) .shell {
  --control-rail: rgba(22, 39, 33, .93);
  --control-rail-edge: rgba(229, 241, 229, .24);
  --control-divider: rgba(229, 241, 229, .18);
  --control-hover: rgba(229, 241, 229, .09);
  --control-ink: #edf4eb;
  --control-mark: #b2e68e;
  --control-shadow: 0 7px 20px rgba(10, 29, 17, .17), inset 0 1px rgba(255, 255, 255, .045);
}
.headerActions {
  gap: 0;
  height: 42px;
  padding: 1px;
  border: 1px solid var(--control-rail-edge);
  border-radius: 11px;
  background: var(--control-rail);
  color: var(--control-ink);
  box-shadow: var(--control-shadow);
  backdrop-filter: blur(18px) saturate(1.05);
  -webkit-backdrop-filter: blur(18px) saturate(1.05);
}
.headerActions > * + * { border-left: 1px solid var(--control-divider); }
.headerActions > .accountMenu { align-self: stretch; display: grid; }
.agentLauncher,
.menuToggle,
.identity,
.themeToggle {
  position: relative;
  border: 0;
  border-radius: 0;
  background: transparent;
  color: var(--control-ink);
  box-shadow: none;
  backdrop-filter: none;
  -webkit-backdrop-filter: none;
  transition: background .18s ease, color .18s ease, opacity .18s ease;
}
.agentLauncher,
.agentMinimized { gap: 8px; align-items: center; justify-content: center; font-weight: 650; letter-spacing: -.01em; }
.agentLauncher { min-width: 82px; height: 38px; padding: 0 13px; border-radius: 9px 0 0 9px; }
.agentLauncher::after {
  content: '';
  position: absolute;
  right: 0;
  bottom: 0;
  left: 0;
  height: 2px;
  background: var(--control-mark);
  opacity: 0;
  transform: scaleX(.55);
  transition: opacity .18s ease, transform .18s ease;
}
.agentLauncher[data-active='true']::after { opacity: 1; transform: scaleX(1); }
.agentLauncher:hover::after { opacity: .55; transform: scaleX(1); }
.agentLauncher[data-active='true']:hover::after { opacity: 1; }
.agentMinimized { position: fixed; right: 18px; bottom: max(18px, env(safe-area-inset-bottom)); z-index: 75; display: inline-flex; min-height: 43px; padding: 0 15px; border-radius: 11px; }
.agentLauncher > span:first-child,
.agentMinimized > span:first-child { color: var(--control-ink); margin: 0; }
.agentLauncher .agentMark,
.agentMinimized .agentMark { width: 18px; height: 17px; margin: 0; background: var(--control-mark); filter: none; }
.menuToggle,
.identity,
.themeToggle { width: 42px; height: 38px; flex-basis: 42px; }
.themeToggle { border-radius: 0 9px 9px 0; }
.menuToggle:hover,
.menuToggle[aria-expanded='true'],
.identity:hover,
.themeToggle:hover,
.agentLauncher:hover { transform: none; background: var(--control-hover); box-shadow: none; }
.menuToggle:active,
.identity:active,
.themeToggle:active,
.agentLauncher:active { transform: none; background: color-mix(in srgb, var(--control-mark) 13%, transparent); }
.agentLauncher:focus-visible,
.menuToggle:focus-visible,
.identity:focus-visible,
.themeToggle:focus-visible { outline: 2px solid var(--control-mark); outline-offset: -3px; z-index: 1; }
.agentMinimized:hover { background: var(--siq-surface-card-hover); }
.menuGlyph { width: 17px; height: 14px; display: grid; align-content: center; gap: 5px; }
.menuGlyph > span { display: block; height: 2px; border-radius: 2px; background: currentColor; transform-origin: center; transition: transform .24s cubic-bezier(.22,1,.36,1), width .24s ease; }
.menuGlyph > span:last-child { width: 69%; justify-self: end; }
.menuToggle:hover .menuGlyph > span:last-child { width: 100%; }
.menuToggle[aria-expanded='true'] .menuGlyph > span:first-child { transform: translateY(3.25px) rotate(45deg); }
.menuToggle[aria-expanded='true'] .menuGlyph > span:last-child { width: 100%; transform: translateY(-3.25px) rotate(-45deg); }
.identity > .identityInitial { position: static; width: 25px; height: 25px; display: grid; place-items: center; border: 1px solid var(--control-rail-edge); border-radius: 50%; padding: 0; background: transparent; color: var(--control-ink); font: 700 12px/1 var(--font-mp-sans, Arial), sans-serif; }
.identity > .identityStatus { position: absolute; right: 5px; bottom: 5px; min-width: 6px; width: 6px; height: 6px; padding: 0; border: 1px solid var(--control-rail); border-radius: 50%; background: var(--control-mark); box-shadow: none; }
.themeGlyph { position: relative; width: 20px; height: 20px; display: block; }
.themeGlyph svg { position: absolute; inset: 0; width: 20px; height: 20px; stroke-width: 1.6; transition: opacity .25s ease, transform .38s cubic-bezier(.22,1,.36,1); }
.sunIcon { opacity: 1; transform: rotate(0) scale(1); }
.moonIcon { opacity: 0; transform: rotate(-35deg) scale(.65); }
:global(:root[data-theme='light']) .sunIcon { opacity: 0; transform: rotate(35deg) scale(.65); }
:global(:root[data-theme='light']) .moonIcon { opacity: 1; transform: rotate(0) scale(1); }
.accountPopover { top: calc(100% + 9px); padding: 8px; border-radius: 11px; border-color: var(--control-rail-edge); background: var(--siq-surface-raised); box-shadow: 0 20px 45px rgba(0,0,0,.25); backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px); }
.accountPopover > a { border-radius: 9px; transition: background .18s ease, transform .18s ease; }
.accountPopover > a:hover { transform: translateX(2px); }
.agentDock { border: 1px solid rgba(220,242,212,.2); border-radius: 16px; box-shadow: 0 26px 65px rgba(0,0,0,.44), 0 5px 18px rgba(0,0,0,.18), inset 0 1px rgba(255,255,255,.12); }
.agentDockExpanded { border-radius: 13px; box-shadow: 0 14px 36px rgba(0,0,0,.16), inset 0 1px rgba(255,255,255,.1); }
@media (max-width: 500px) { .agentDock { border-radius: 15px; box-shadow: 0 20px 52px rgba(0,0,0,.53), 0 0 0 1px rgba(190,223,179,.04); } .agentDockExpanded { border-radius: 0; box-shadow: none; } }
@media (max-width: 390px) { .agentLauncher { min-width: 40px; width: 40px; padding: 0; } .agentLauncher > span:first-child { display: none; } }
@media (max-width: 360px) {
  .header { gap: 4px; padding-inline: 6px; }
  .brand { gap: 5px; padding-inline: 2px; }
  .brand strong { font-size: 17px; }
  .agentLauncher,.menuToggle,.identity,.themeToggle { width: 38px; height: 38px; min-width: 38px; flex-basis: 38px; }
  .agentDock { bottom: 8px; height: min(390px, calc(100dvh - 84px)); }
}
@media (prefers-contrast: more) {
  .shell { --control-rail: #111713; --control-rail-edge: #c5d4c8; --control-divider: #8da092; }
  :global(:root[data-theme='light']) .shell { --control-rail: #14271c; --control-rail-edge: #d5e4d7; --control-divider: #a8c1ad; }
}
@media (prefers-reduced-motion: reduce) {
  .agentLauncher,.agentMinimized,.menuToggle,.identity,.themeToggle,.menuGlyph > span,.themeGlyph svg,.accountPopover > a { transition-duration: .01ms; }
}

.marketTicker{height:38px;display:flex;align-items:stretch;min-width:0;border-bottom:1px solid var(--siq-line-subtle);background:color-mix(in srgb,var(--siq-field-ground) 88%,transparent);color:var(--siq-text-secondary);font:10px/1 var(--font-mp-mono,'IBM Plex Mono',monospace);letter-spacing:.075em;box-shadow:inset 0 1px rgba(255,255,255,.025)}
.marketTicker:has(.marketPickerPopover){position:relative;z-index:80}
.marketTickerLabel,.marketTickerStatus{position:relative;z-index:1;flex:none;display:flex;align-items:center;white-space:nowrap;background:var(--siq-field-ground)}
.marketTickerLabel{gap:9px;padding:0 16px;color:var(--siq-accent-focus);font-weight:700;border-right:1px solid var(--siq-line-subtle)}
.marketTickerLabel>span{width:5px;height:5px;border-radius:50%;background:currentColor;box-shadow:0 0 9px currentColor}
.marketTickerStatus{padding:0 14px;color:var(--siq-text-tertiary);border-left:1px solid var(--siq-line-subtle)}
.marketTickerStatus>.marketTickerStale{margin-right:7px}
.marketTickerStale{color:var(--siq-status-warning);white-space:nowrap}
.marketPicker{position:relative;display:flex;align-items:center}
.marketPickerTrigger{height:30px;max-width:180px;min-width:110px;display:flex;align-items:center;gap:7px;padding:0 9px;border:1px solid var(--siq-line-subtle);border-radius:6px;background:rgba(255,255,255,.035);color:var(--siq-text-primary);font:600 10px var(--font-mp-mono,'IBM Plex Mono',monospace);letter-spacing:.02em;cursor:pointer;transition:background .16s,border-color .16s,box-shadow .16s}
.marketPickerTrigger:hover,.marketPickerTrigger[aria-expanded='true']{background:rgba(255,255,255,.075);border-color:var(--siq-line-strong);box-shadow:inset 0 1px rgba(255,255,255,.06)}
.marketPickerTrigger:focus-visible{outline:2px solid var(--siq-accent-focus);outline-offset:2px}
.marketPickerTrigger svg:first-child{width:13px;height:13px;flex:none;color:var(--siq-accent-primary)}
.marketPickerTrigger svg:last-child{width:11px;height:11px;flex:none;margin-left:auto;color:var(--siq-text-tertiary);transition:transform .16s}
.marketPickerTrigger[aria-expanded='true'] svg:last-child{transform:rotate(180deg)}
.marketPickerTrigger>span{min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;margin:0}
.marketPickerPopover{position:absolute;top:calc(100% + 8px);right:-8px;z-index:120;width:min(312px,calc(100vw - 20px));padding:9px;border:1px solid var(--siq-line-strong);border-radius:10px;background:color-mix(in srgb,var(--siq-field-raised) 92%,transparent);box-shadow:0 20px 50px rgba(0,0,0,.42),inset 0 1px rgba(255,255,255,.06);backdrop-filter:blur(24px) saturate(1.2);letter-spacing:0}
.marketPickerSearch{height:36px;display:flex;align-items:center;gap:8px;padding:0 10px;border:1px solid var(--siq-line-default);border-radius:6px;background:var(--siq-surface-input)}
.marketPickerSearch:focus-within{border-color:var(--siq-line-strong);box-shadow:0 0 0 2px rgba(127,211,163,.09)}
.marketPickerSearch svg{width:14px;height:14px;flex:none;color:var(--siq-text-tertiary)}
.marketPickerSearch input{width:100%;height:100%;min-width:0;border:0;outline:0;background:transparent;color:var(--siq-text-primary);font:12px var(--font-mp-sans,Arial),sans-serif}
.marketPickerSearch input:focus-visible{outline:0!important;box-shadow:none!important}
.marketPickerSearch input::placeholder{color:var(--siq-text-tertiary)}
.marketPickerList{max-height:min(340px,calc(100dvh - 180px));overflow-y:auto;overscroll-behavior:contain;margin-top:7px;scrollbar-color:var(--siq-line-strong) transparent;scrollbar-width:thin}
.marketPickerGroup{display:grid;gap:2px;padding:3px 0 7px}
.marketPickerGroup+.marketPickerGroup{border-top:1px solid var(--siq-line-subtle);padding-top:9px}
.marketPickerGroupLabel{padding:5px 9px;color:var(--siq-text-tertiary);font:700 9px var(--font-mp-mono,'IBM Plex Mono',monospace);letter-spacing:.12em}
.marketPickerOption{width:100%;min-height:38px;display:flex;align-items:center;gap:10px;text-align:left;padding:0 9px;border:0;border-radius:5px;background:transparent;color:var(--siq-text-secondary);font:13px var(--font-mp-sans,Arial),sans-serif;cursor:pointer}
.marketPickerOption:hover,.marketPickerOption:focus-visible{outline:0;background:var(--siq-surface-card-hover);color:var(--siq-text-primary)}
.marketPickerOption[data-selected='true']{background:var(--siq-surface-selected);color:var(--siq-accent-primary)}
.marketPickerOption>span{min-width:0;flex:1;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;margin:0}
.marketPickerOption small{color:var(--siq-text-tertiary);font:9px var(--font-mp-mono,'IBM Plex Mono',monospace)}
.marketPickerOption svg{width:12px;height:12px;flex:none}
.marketPickerEmpty{margin:0;padding:16px 9px;color:var(--siq-text-tertiary);font:12px var(--font-mp-sans,Arial),sans-serif}
.marketPickerFoot{margin:8px 2px 2px;padding:8px 6px 2px;border-top:1px solid var(--siq-line-subtle);color:var(--siq-text-tertiary);font:10px var(--font-mp-sans,Arial),sans-serif}
.marketTickerViewport{position:relative;flex:1;min-width:0;overflow:hidden;mask-image:linear-gradient(90deg,transparent,#000 3%,#000 97%,transparent)}
.marketTickerTrack{display:flex;width:max-content;height:100%;animation:marketTickerScroll 42s linear infinite}
.marketTickerGroup{display:flex;align-items:center;flex:none;height:100%}
.marketTickerItem{display:inline-flex;align-items:center;gap:10px;height:100%;padding:0 24px;border-right:1px solid var(--siq-line-subtle);white-space:nowrap}
.marketTickerItem:is(a){color:inherit;text-decoration:none}.marketTickerItem:is(a):hover .marketTickerName,.marketTickerItem:is(a):focus-visible .marketTickerName{color:var(--siq-text-primary)}
.marketTickerName{color:var(--siq-text-secondary);text-transform:uppercase;font-weight:600}
.marketTickerValue{color:var(--siq-accent-primary);font-size:11px;font-weight:700;letter-spacing:0}
.marketTickerRegion{color:var(--siq-text-tertiary);font-size:9px}
.marketTickerPeriod{color:var(--siq-text-tertiary);font-size:9px;letter-spacing:0}
.marketTicker:hover .marketTickerTrack,.marketTicker:focus-within .marketTickerTrack{animation-play-state:paused}
@keyframes marketTickerScroll{to{transform:translateX(-50%)}}
@media(max-width:600px){.marketTickerLabel{padding:0 10px;font-size:0;gap:0}.marketTickerLabel>span{width:6px;height:6px}.marketTickerStatus{padding:0 7px;font-size:9px}.marketTickerStatus>.marketTickerStale{display:none}.marketPickerTrigger{max-width:113px;min-width:93px;padding:0 7px;gap:5px;font-size:9px}.marketPickerPopover{right:-7px}.marketTickerItem{padding:0 16px;gap:7px}.marketTickerRegion{display:none}}
@media(prefers-reduced-motion:reduce){.marketTickerViewport{overflow-x:auto;mask-image:none}.marketTickerTrack{animation:none}.marketTickerGroup[aria-hidden='true']{display:none}}
